package NewMortgageCalculator;

import java.util.Scanner;

public class NewMortgageCalculator {
    public static void main(String[] args) {
        // greetUser("Gavin");

        // String phrase = "Ener a number between 1,000 and 1,000,000.";
        // String principalPhrase = "Principal ($1k - $1M)";

        // String annualPhrase = "Enter a value greater than 0 and greater than or equal
        // to 30";
        // String annualInterestPhrase = "Annual interest rate: ";

        // String yearPhrase = "Period (Years): ";

        // Scanner userInput = new Scanner(System.in);
        // int inputtedNumber = 0;

        // while (true) {
        // System.out.println(principalPhrase);
        // inputtedNumber = userInput.nextInt();
        // if (inputtedNumber >= 1000 && inputtedNumber <= 1_000_000) {
        // break;
        // }
        // System.out.println(phrase);

        // }
        // float annualInterestRate = 0;
        // while (true) {
        // System.out.println(annualInterestPhrase);
        // annualInterestRate = userInput.nextFloat();
        // if (annualInterestRate > 0 && annualInterestRate <= 30) {
        // break;
        // }
        // System.out.println(annualPhrase);
        // }
        // int years = 0;
        // while (true) {
        // System.out.println(yearPhrase);
        // years = userInput.nextInt();
        // if (years > 0 && years <= 30) {
        // break;
        // }
        // System.out.println(annualPhrase);

        // }
        // double result = (inputtedNumber * (annualInterestRate / 100)) * years;
        // System.out.println(result);

        // userInput.close();
    }

    // public static void greetUser(String username) {
    // System.out.println("Hello " + username);
    // }

}
