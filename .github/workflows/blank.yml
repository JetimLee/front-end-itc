name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello from action"
      - name: Print goodbye
        run: echo "Done - goodbye!"
      
